#!/usr/bin/env bash
# This script displays all active IPv4 IP addresses on the machine.

# Use the `ip` command to list all IP addresses associated with active network interfaces.
# Filter out IPv6 addresses and display only IPv4 addresses.
# The output is formatted to show only the IP addresses.

ip -4 -o addr show up | awk '{print $4}' | cut -d/ -f1

